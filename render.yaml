services:
  - type: web
    name: converter
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn backend.main:app -k uvicorn.workers.UvicornWorker -c gunicorn.conf.py
    healthCheckPath: /app/
    autoDeploy: true